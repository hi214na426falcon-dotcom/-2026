// =====================================================================
// Supabase 接続設定  ——  無料プロジェクトを作成したあとに記入してください。
// Supabase ダッシュボード → Settings → API から:
//   ・Project URL           → SUPABASE_URL
//   ・Project API keys の anon public → SUPABASE_ANON_KEY
//
// anon(公開)キーはブラウザに置いても安全です（データは行レベル権限=RLSで保護）。
// 空のままだと、アプリは「ローカルモード」で動作します（この端末のみ・共有なし）。
// =====================================================================
window.WHIMO_ENV = {
  SUPABASE_URL: '',
  SUPABASE_ANON_KEY: '',
};
