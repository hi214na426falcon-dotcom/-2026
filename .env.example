# このファイルを .env という名前でコピーして、実際のAPIキーを記入してください:
#   cp .env.example .env
#
# APIキーの取得先: https://aistudio.google.com/apikey
# .env は .gitignore に登録済みのため、Gitにはコミットされません。

# Gemini API キー(Nano Banana 2 = 画像生成モデルへのアクセスに使用)
GEMINI_API_KEY=
